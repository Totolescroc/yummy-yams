Si la collection pastries n'est pas présente : 

npm run import:pastries


Créer un .env avec vos identifiant pour la db et copier le également dans le dossier api/

PORT=??
MONGO_URI=mongodb://??
JWT_SECRET=your_secret_key
